import styled from 'styled-components';

export const ImportContenedor = styled.div`
  display: grid;
  place-items: center;
  width: 50%;
  height: 50%;
  padding: var(--space-fluid-3);
  border: 5px dotted var(--surface-fourth);
  border-radius: 10px;
`;
